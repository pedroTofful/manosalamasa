{% extends 'base.html.twig' %}

{% block title %}Restriccion index{% endblock %}

{% block body %}
    <h1>Restriccion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>RestriccionDsc</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for restriccion in restriccions %}
            <tr>
                <td>{{ restriccion.id }}</td>
                <td>{{ restriccion.restriccionDsc }}</td>
                <td>
                    <a href="{{ path('restriccion_show', {'id': restriccion.id}) }}">show</a>
                    <a href="{{ path('restriccion_edit', {'id': restriccion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('restriccion_new') }}">Create new</a>
{% endblock %}
