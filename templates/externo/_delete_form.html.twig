<form method="post" action="{{ path('externo_delete', {'id': externo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ externo.id) }}">
    <button class="btn">Delete</button>
</form>
