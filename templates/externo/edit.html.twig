{% extends 'base.html.twig' %}

{% block title %}Edit Externo{% endblock %}

{% block body %}
    <h1>Edit Externo</h1>

    {{ include('externo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('externo_index') }}">back to list</a>

    {{ include('externo/_delete_form.html.twig') }}
{% endblock %}
