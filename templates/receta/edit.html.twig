{% extends 'base.html.twig' %}

{% block title %}Edit Receta{% endblock %}

{% block body %}
    <h1>Edit Receta</h1>

    {{ include('receta/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('receta_index') }}">back to list</a>

    {{ include('receta/_delete_form.html.twig') }}
{% endblock %}
