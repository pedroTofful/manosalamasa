{% extends 'base.html.twig' %}

{% block title %}Ingrediente{% endblock %}

{% block body %}
    <h1>Ingrediente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingrediente.id }}</td>
            </tr>
            <tr>
                <th>IngredienteDsc</th>
                <td>{{ ingrediente.ingredienteDsc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ingrediente_index') }}">back to list</a>

    <a href="{{ path('ingrediente_edit', {'id': ingrediente.id}) }}">edit</a>

    {{ include('ingrediente/_delete_form.html.twig') }}
{% endblock %}
