<form method="post" action="{{ path('ingrediente_delete', {'id': ingrediente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingrediente.id) }}">
    <button class="btn">Delete</button>
</form>
