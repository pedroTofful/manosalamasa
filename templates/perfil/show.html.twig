{% extends 'base.html.twig' %}

{% block title %}Perfil{% endblock %}

{% block body %}
    <h1>Perfil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ perfil.id }}</td>
            </tr>
            <tr>
                <th>PerfilNomApe</th>
                <td>{{ perfil.perfilNomApe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('perfil_index') }}">back to list</a>

    <a href="{{ path('perfil_edit', {'id': perfil.id}) }}">edit</a>

    {{ include('perfil/_delete_form.html.twig') }}
{% endblock %}
