<form method="post" action="{{ path('perfil_delete', {'id': perfil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perfil.id) }}">
    <button class="btn">Delete</button>
</form>
